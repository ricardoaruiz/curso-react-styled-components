import * as vars from './variables'

export const lightTheme = {
  body: vars.lightBackground,
  inside: vars.lightContent,
  text: vars.textColorLightBackground
}

export const darkTheme = {
  body: vars.darkBackground,
  inside: vars.darkContent,
  text: vars.textColorDarkBackground
}